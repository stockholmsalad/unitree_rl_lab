# JE-Loco 연구 정리 — 발표용 자료

**작성 2026-08-20** · Unitree Go2 · RealSense D435i depth 단독
저장소: `unitree_rl_lab` (branch `pilab`) · 학습 서버: 9960x (RTX PRO 6000 Blackwell 97GB)

---

## 0. 목표 · 기여 · 핵심 (발표 도입부)

### 0.1 문제

사족보행 로봇이 지형을 보려면 외수용 감각이 필요하다. 그런데 학습에 쓰이는
**GT 높이맵은 시뮬레이터에만 존재한다** — 실제 로봇에는 그런 센서가 없다.
게다가 실기의 depth 센서는 **실제로 망가진다**: 반사면에 구멍이 뚫리고, 다리가 시야를 가리고,
USB 대역폭이 모자라 프레임이 빠지고, 카메라는 30fps인데 제어는 50Hz다.

### 0.2 목표

> **시뮬레이터 특권 정보 없이, 로봇이 실제로 얻을 수 있는 신호만으로 표현을 학습해
> 특권 정보 기반과 동등한 보행 성능에 도달하는 것.**
> 그리고 그 표현이 **어떤 센서 고장에 강한지를 규명하는 것.**

### 0.3 기여 (Contributions)

| # | 기여 | 근거 |
|---|---|---|
| **C1** | **특권 정보 없이 동등 이상의 성능.** 자기지도 잠재 예측(JEPA)으로 학습한 인코더가, GT 높이맵을 쓴 재구성 인코더와 동등하거나 **더 낫다** | 무결손 성공률 **jepa 97.9 vs recon 95.85** (조건내 시드폭 0.1~0.4pp) |
| **C2** | **점군 기반 DAgger 증류 파이프라인.** 학생이 teacher 수준으로 걷는다 — 표현 비교가 성립하는 토대 | ep_len 962~971 (teacher 948~979), 넘어짐 2.8~3.2% |
| **C3** | **결손 벤치마크 설계 원칙.** i.i.d. dropout은 PointNet masked max-pool이 흡수해 **판정력이 없다**. 공간 구조를 가진 결손(occlusion)만 조건을 가른다 | dropout AULC 0.859~0.870 전부 동률 vs occlusion 0.352~0.487 |
| **C4** | **사전학습 과제가 강인해질 고장 종류를 결정한다.** 공간 복원을 배우면 공간 결손에, 시간 예측을 배우면 시간 단절에 강해진다 | occlusion에서 recon > scratch > **jepa**(랜덤보다도 나쁨). 시간 결손 검증 진행 중 |
| **C5** | **실험 위생 사례집.** 판정을 실제로 뒤집었던 도구·설계 결함 6건 | §7 |

### 0.4 핵심 메시지 (한 줄)

> **특권 정보는 없어도 된다. 다만 "어떤 자기지도 과제를 고르느냐"가
> "어떤 센서 고장에 강해지느냐"를 결정한다.**

C4가 이 연구의 지적 핵심이다. 강인성 trade-off는 **약점이 아니라 발견**이다 —
사전학습 과제(pretext task)와 배포 환경의 고장 양상을 **맞춰야 한다**는 설계 원칙을 준다.

### 0.5 지금 검증 중인 것

C4의 나머지 절반. **시간 예측을 배운 jepa는 시간적 결손에 강해야 한다**는 예측을
사전 등록하고 검증 중 (`freeze` / `latency` / `lowfps`, §5.1).
맞으면 C4가 대칭으로 완성되고, **틀리면 틀렸다고 보고한다.**

---

## 1. 한 문장 주장

> **시뮬레이터 특권 정보(GT 높이맵) 없이 센서 스트림만으로 자기지도 학습한 잠재 예측 표현으로,
> 특권 정보를 쓴 재구성 기반과 동등한 보행 성능을 얻는다.**

부수 주장: 강인성에는 **결손 종류에 따른 trade-off**가 있다.

### 1.1 왜 이게 값어치 있나

| | 사전학습 목적함수 | 감독 신호 | 실기에서 학습 가능? |
|---|---|---|---|
| 재구성 (Head A) | z_e → GT 높이맵(144) | **특권** (시뮬 raycaster) | ❌ 시뮬 전용 |
| **JEPA (Head B)** | z_e(t) → z_e(t+k) | **자기지도** (EMA 타깃) | ✅ 무라벨 실기 데이터 |
| scratch | 없음 | — | ✅ |

핵심은 **"decoder가 없어서 가볍다"가 아니다**(그건 성립하지 않는다 — §6.1 참조).
**"라벨이 필요 없다"**가 핵심이다.

---

## 2. 전체 파이프라인 (3단계)

```
┌─ Phase 1: Teacher 학습 (PPO) ────────────────────────────────────┐
│  관측 = proprio 45 + privileged height_scan 187 (17×11) = 232    │
│  구조 = MLP actor-critic, 20,000 iter, num_steps_per_env=24      │
│  결과 = 잘 걷는 특권 정책 (ep_len 948~979)                        │
│  ※ 실기 배포 불가 — height_scan 은 시뮬에만 존재                   │
└──────────────────────────┬───────────────────────────────────────┘
                           │ teacher 로 데이터 수집 (collect_teacher_data.py)
                           ▼
┌─ Phase 2: 표현 사전학습 (오프라인, 순수 PyTorch) ──────────────────┐
│  입력 = 수집된 점군 시퀀스                                         │
│  jepa_v1  : z_e(t) → Δz_e(t+k),  k ∈ {5,15,25,50} 샘플            │
│             + EMA target(τ=0.996) + VICReg(λ_var 1.0, λ_cov 0.04) │
│             + 행동 조건화(다운샘플 8) + 결손 증강(최대 30%)         │
│  recon_v1 : z_e → GT 높이맵 회귀 (특권 대조군)                     │
│  산출 = pretrained/{jepa_v1,recon_v1}/pc_encoder.pt               │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 인코더 가중치를 학생에 로드
                           ▼
┌─ Phase 3b: DAgger 증류 (★ 현재 단계) ─────────────────────────────┐
│  환경을 **학생 행동**으로 굴리고, 방문 상태마다 teacher 질의        │
│  손실 = MSE(student(obs), a_teacher)                              │
│  학생 관측 = proprio 45×H(5) + 점군 192점 (배포 가능한 것만)        │
│  8,000 iter · 1024 env · num_steps_per_env=32                     │
│                                                                   │
│  ★ 비교 축 = 학생 인코더 초기화 **단 하나**                        │
│     jepa_v1 / recon_v1 / scratch(랜덤)  × seed                    │
│     나머지(teacher·환경·하이퍼파라미터·평가 지형) 전부 동일         │
└───────────────────────────────────────────────────────────────────┘
```

### 2.1 왜 Phase 3(동결+PPO)에서 3b(증류)로 바꿨나

Phase 3은 사전학습 인코더를 **동결하고 PPO로 정책만** 재학습하는 설계였다.
결과: **세 조건 모두 제대로 걷지 못했다.** teacher는 잘 걷는데 학생은 인코더를 뭘 쓰든 실패.

원인 = **행동 감독과 on-policy 보정을 둘 다 뺐기 때문**.
문헌 근거: 「Now You See That」 — depth end-to-end RL 54.0% vs privileged distillation **98.9%**.

> 둘 다 못 걷는 정책끼리 표현을 비교하는 건 답할 수 없는 질문이다.
> → 표준 레시피인 **DAgger**로 전환. 연구 질문(인코더 초기화 비교)은 그대로 유지.

DeFM(2026)이 같은 설계다 — 동결 DeFM vs scratch CNN을 distillation 안에서 비교.

---

## 3. 아키텍처 상세

### 3.1 학생 정책 (배포 대상)

```
점군 192점 [x,y,z,valid]        proprio 45×5 히스토리
        │                              │
   PointNet-lite                 proprio encoder
   3→32→64→128                   225→128→32
   masked max-pool ★              │
   128→64                         │
        │ z_e (64)                │ z_p (32)
        └──────────┬──────────────┘
                   ▼
            GRU (hidden 256, 1층)
                   ▼
            MLP 256→128 → action 12
```

★ **masked max-pool**: `valid=0`인 점은 −inf로 제외 → 어떤 좌표도 pooling에 주입되지 않음.
(과거 sentinel 좌표 주입이 max-pool hijack 아티팩트를 만들어 결과를 오염시킨 적 있음.)

보조 출력: `v̂ = vel_decoder(z_p)` — 두 조건 공통(CENet 방식), base 선속도 추정.

### 3.2 표현 헤드 (학습 시에만 존재, 배포 시 폐기)

| | 구조 | 파라미터 |
|---|---|---|
| **Head A (recon)** | z_e(64) → 128 → height(144) | 26,896 |
| **Head B (jepa)** | predictor 96→128→64 (20,672) + EMA target encoder(18,816) + VICReg projector 64→128→128 (24,832) | **64,320** |

### 3.3 센서 사양

- **pc_scanner**: D435i 모사 프러스텀 raycaster. 전방 32.5cm·위 4.5cm 장착, **35° 하향**,
  FoV 78.7°×63.1°, max 3m. **16×12 = 192점** (실기 64×48 다운샘플). 시야 x ∈ [0.5, 2.3] m
- **height_scanner** (Head A GT, 특권): 전방 x∈[0.5,2.0]·y∈[−0.4,0.4], **16×9 = 144**
- **teacher_scanner** (teacher 관측, 특권): **17×11 = 187**
- proprio 45차원 = `[ω(3), g(3), cmd(3), θ(12), θ̇(12), a_{t-1}(12)]`
  — base 선속도 v_t는 **넣지 않는다**(실기 드리프트). critic에만 GT 제공 + z_p로 추정

### 3.4 환경

- 명령: `lin_vel_x ∈ (0.3, 0.6)`, limit (0.3, 1.0). **lin_vel_y = 0, ang_vel_z = 0 — 직진 전용**
- 에피소드 20초 = **1000 스텝** (제어 50Hz)
- 커리큘럼: `scripted_terrain_levels` — 난이도 상한을 iteration 함수로 **고정**
  (표준 `terrain_levels_vel`은 성능 의존이라 조건마다 훈련 지형이 달라지는 내생적 교란)

---

## 4. 결과

### 4.1 증류 자체는 성공 (기여 1)

| | ep_len | 넘어짐 | 자세 이탈 |
|---|---|---|---|
| teacher | 948~979 | — | 0.68 |
| 학생 (전 조건) | **962~971** | 2.8~3.2% | **0.56** |

Phase 3 PPO는 5.9~7.7% 넘어짐. **학생이 teacher 수준으로 걷는다** — 표현 비교의 전제가 성립.

### 4.2 모방 정확도 (`Loss/behavior`, 8000 iter, n=2)

| 조건 | s1 | s2 | 평균 |
|---|---|---|---|
| recon | 0.0501 | 0.0501 | **0.0501** |
| jepa | 0.0518 | 0.0536 | 0.0527 |
| scratch | 0.0544 | 0.0536 | 0.0540 |

- 최대 시드폭 0.0018. recon–jepa Δ=0.0025(신호), jepa–scratch Δ=0.0013(**노이즈 안**)
- **"사전학습 > scratch"는 iter 5400에서 보였다가 8000에서 증발** (jepa_s2 = scratch_s2 = 0.0536)
- 수렴 속도는 사전학습 우위 유지 (`<0.060` 도달: recon 2729/2686 · jepa 3243/1906 · scratch 4663/4352)

### 4.3 무결손 성능 — ★ 주 주장의 근거

| 조건 | level 0 성공률 |
|---|---|
| **jepa** | **97.7 / 98.1** ← 최고 |
| scratch | 97.0 / 96.6 |
| recon | 95.9 / 95.8 ← 최저 |

**JEPA가 특권 정보 없이도 재구성보다 낫다.** 조건 내 시드폭 0.1~0.4pp vs 조건 간 2pp → 신호.

### 4.4 결손 강인성 (그림: `results/distill_degradation/degradation_3cond.png`)

| 결손 | 판정력 | 순위 (AULC) |
|---|---|---|
| **dropout** (i.i.d. 점) | ❌ **없음** | 0.859~0.870, 전부 동률 |
| **hole** (블록) | 약함 | recon 0.741 > scratch 0.720 > jepa 0.638 |
| **occlusion** (하단 대역) | ✅ **있음** | **recon 0.487 > scratch 0.433 > jepa 0.352** |

- occlusion에서 **recon 두 시드가 나머지 넷보다 전부 위**(6/6 무겹침). @0.6에서 recon 최저(24.9%)가
  차순위(scratch 8.1%)의 **3배**
- **jepa가 랜덤 초기화보다도 나쁘다** — scratch–jepa Δ=0.081 > 시드폭 0.077 (유일하게 엄격 기준 통과)
- **dropout이 판정력 없는 이유**: PointNet masked max-pool이 i.i.d. 결손을 흡수 → **방법론적 발견**

### 4.5 세 지표가 서로 다른 순위를 낸다 (논문에 병기 필수)

| 지표 | 1위 | 꼴찌 |
|---|---|---|
| 모방 정확도 | recon | scratch |
| 무결손 성공률 | **jepa** | recon |
| occlusion 강인성 | recon | **jepa** |

---

## 5. 진행 중 / 계획

### 5.1 게이트 3 — 시간적 결손 (2026-08-20 추가, 사전 등록됨)

**가설은 방법의 정의에서 유도된다** (데이터를 보고 고른 지표가 아님):

> jepa는 `z_e(t) → z_e(t+k)`를 학습했다. 따라서 **관측이 시간적으로 끊기는 고장**에서 우위가 있어야 한다.
> recon은 현재 프레임에서 높이맵을 복원할 뿐이므로, 새 프레임이 없으면 쓸 능력이 없다.

| 스트레서 | 동작 | level 1.0 |
|---|---|---|
| `freeze` | 확률 level로 프레임 갱신 실패 | 카메라 완전 정지 |
| `latency` | level×10 스텝 지연 | 0.2s 지연 |
| `lowfps` | stride = 1+level×9 | 50Hz → 5Hz |

**실기 D435i는 30fps인데 제어는 50Hz — 이미 이 축 위에 있다.**

**스모크 결과 (jepa_s1)**: level 0 성공률 97.7%(기존과 소수점까지 일치 → 항등 검증),
freeze 1.0 → **85.6%**. 결손은 주입되나 **동적 범위가 12pp뿐** → 기본 지형(평균 레벨 1.43)에서는
로봇이 시각을 거의 안 쓴다. → **어려운 지형(레벨 5) 재평가 단계 추가**.

### 5.2 무인 실행 큐 (7일)

| Stage | 내용 | 소요 |
|---|---|---|
| A | 시드 증설 — 3조건 × seed{3,4,5} = 9런 | ~2~3일 |
| B | 15런 × 4결손, 기본 지형 = 60회 평가 | ~6h |
| C | 같은 조합을 **지형 레벨 5**로 | ~6~8h |

### 5.3 남은 필수 작업

1. **시드 증설 n=5** — 현재 모든 결론의 p ≈ 6.7%(6개 중 특정 2개가 상위 2등일 확률 1/15)
2. **Head C (점군 자기재구성)** — 주장 ②의 대조군. 심사 예상 질문:
   *"라벨이 필요 없는 게 장점이라면, 점군 오토인코더는 왜 안 되나?"* 현재 답할 수 없음

```
        │ 관측공간 재구성        │ 잠재공간 예측
────────┼──────────────────────┼──────────────────
특권    │ Head A (GT 높이맵)    │ —
자기지도 │ ★ Head C (비어있음)   │ Head B (JEPA)
```
- **A vs C** → 특권 정보가 실제로 도움이 되는가
- **C vs B** → 라벨 없이, 관측 복원 vs 잠재 예측 (= **I-JEPA의 실제 주장**)

---

## 6. 반드시 정확히 써야 할 것 (심사 방어)

### 6.1 "decoder가 없어서 가볍다" — 성립하지 않음

- **배포 시 두 조건이 완전히 동일하다.** 두 헤드 모두 학습 전용, 추론 시 폐기. 파라미터 차이 0
- **학습 시엔 JEPA가 2.39배 무겁다** (64,320 vs 26,896) — EMA target encoder 사본 + VICReg projector
- DreamWaQ도 CVAE decoder를 학습 때만 쓰고 배포 시 버린다 — **decoder 폐기는 이 분야 표준**
- → 차별점은 "decoder 유무"가 아니라 **"복원 대상이 무엇이냐"**.
  DreamWaQ = 다음 고유수용 관측(자기지도) / 우리 Head A = **GT 높이맵(특권)**

### 6.2 "JEPA"라는 이름의 사정거리

| | I-JEPA (Assran et al. 2023) | 이 구현 |
|---|---|---|
| 예측 대상 | 같은 이미지 안의 **가려진 공간 블록** | **k스텝 뒤 시점** |
| predictor 조건 | 예측할 **위치**의 mask token | z_p, 명령, 평균 행동 |
| EMA target + 표현공간 L2 | ✓ | ✓ |
| collapse 방어 | 구조적 비대칭만 | **VICReg 추가** |

- 지금 구현은 I-JEPA가 아니라 **SPR(Schwarzer et al. 2021) 계열 시간 예측**
- 단, LeCun의 **넓은 JEPA 정의**(표현공간에서 잠재 조건 predictor로 예측)에는 부합 →
  "JEPA 계열이되 I-JEPA가 아닌 시간 예측 변형"으로 명시하면 방어됨
- **결론을 "예측 일반"이 아니라 "시간 예측"으로 한정할 것**

### 6.3 알려진 교란: 축이 두 개 섞여 있다

Head A(관측공간·현재)와 Head B(표현공간·미래)는 2×2의 **대각선**이라, 차이의 원인이
공간인지 시간인지 구분되지 않는다. → 한계로 명시 + Head C/D를 후속으로 제시.

### 6.4 통계 규율

- n=2 결과는 **확정으로 쓰지 않는다**
- 동등성 마진 **2pp** 사전 선언 (관측 시드폭 0.1~0.4pp 기준 n=5면 검정력 충분)
- 사전 등록 문서: `source/unitree_rl_lab/unitree_rl_lab/je_loco/PREREGISTRATION.md`

---

## 7. 실험 위생 사례집 (기여로 쓸 수 있음)

이 프로젝트에서 **판정을 실제로 뒤집었던** 도구·설계 결함들:

| # | 결함 | 증상 | 영향 |
|---|---|---|---|
| 1 | **sentinel 좌표 주입** | 무효 점에 좌표를 넣어 masked max-pool을 hijack | ICCAS "A>B" 결과가 **아티팩트로 판명** |
| 2 | **거미 자세 gait 버그** | 비정상 자세로 걷는 정책 | "사전학습 2.8배 빠름" 6/6 분리가 **증발** |
| 3 | **play 지형 항상 레벨 0** | `scripted_terrain_levels`가 play에서 상한 1 → `max_init_terrain_level` 무시 | **모든 육안 판정이 사실상 평지에서** 이뤄짐 |
| 4 | **증류 체크포인트 미로드** | 키가 `student_state_dict`인데 `OnPolicyRunner`로 로드, `strict=False`라 조용히 통과 | 정책 미로드 = "가만히 서 있음" → **학습 실패와 구분 불가** |
| 5 | **eval `--steps` < 에피소드 길이** | timeout이 구조적으로 0건 → 넘어짐 1건에도 성공률 0% | 스모크 판정 **무의미** |
| 6 | **커리큘럼 divergence** | 조건마다 훈련 지형 난이도가 달라짐 | raw reward 비교 **오염** |

> **교훈: 도구도 검증 대상이다.** 4번은 발견 못 했으면 "세 조건 모두 동일하게 취약"이라는
> 가짜 무승부를 만들어 결론을 정확히 반대로 뒤집었을 것.

---

## 8. 발표 슬라이드 제안 구성

| # | 슬라이드 | 내용 | 참조 |
|---|---|---|---|
| 1 | **문제** | GT 높이맵은 시뮬에만 있다 + 실기 depth는 실제로 망가진다(구멍·차폐·프레임드롭·30fps) | §0.1 |
| 2 | **목표** | 특권 정보 없이 동등 성능 + 어떤 고장에 강한지 규명 | §0.2 |
| 3 | **기여 5가지** | C1~C5 표 한 장 | §0.3 |
| 4 | **파이프라인** | Teacher(PPO) → 표현 사전학습 → DAgger 증류 | `architecture.png` ① |
| 5 | **왜 DAgger인가** | Phase 3(동결+PPO)은 세 조건 다 못 걸어 무효. 문헌: 54.0% vs 98.9% | §2.1 |
| 6 | **통제 설계** | 백본 고정, **인코더 초기화만** 교체 | `architecture.png` ②③ |
| 7 | **결과 1 — 증류 성공** | 학생이 teacher 수준으로 걷는다 (C2) | §4.1 |
| 8 | **결과 2 — 특권 정보 불필요** | 무결손 jepa 97.9 vs recon 95.85 (C1) ← **주 결과** | §4.3 |
| 9 | **결과 3 — 벤치마크 설계** | dropout은 판정력 없음, occlusion만 가른다 (C3) | §4.4, `results_degradation.png` |
| 10 | **결과 4 — 과제↔고장 정합** | 공간 복원 → 공간 결손에 강함. jepa는 랜덤보다도 약함 (C4) | §4.4 |
| 11 | **핵심 메시지** | "어떤 자기지도 과제를 고르느냐가 어떤 고장에 강해지느냐를 결정한다" | §0.4 |
| 12 | **검증 중** | 시간 결손 예측 — 사전 등록하고 돌리는 중 | §5.1 |
| 13 | **한계와 후속** | 축 혼재(2×2 대각선), Head C, n=5 증설 | §5.3, §6.3 |
| 14 | **실험 위생** | 판정을 뒤집은 6가지 ← 차별화 포인트 | §7 |

**질문 대비**: §6(심사 방어) 네 항목 — "가볍다" 주장 철회 / I-JEPA가 아닌 이유 /
축 혼재 / 통계 규율. 이 넷은 반드시 먼저 알고 들어가야 한다.

### 첨부 그림

- `results/distill_degradation/degradation_3cond.png` — 3조건 × 2시드 결손 곡선 (3패널)
- `docs/figs/architecture.png` — 파이프라인 + 학생 아키텍처 (본 문서와 함께 생성)
